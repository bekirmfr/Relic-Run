{
  "name": "the versus lobby and the duel's last word",
  "mutants": [
    {
      "name": "a rival's level is not banded around the hero's",
      "file": "Assets/GameAssets/Core/Run/VersusRun.cs",
      "find": "int level = Math.Max(1, heroLevel + rng.NextInt(3) - 1);",
      "replace": "int level = Math.Max(1, heroLevel + rng.NextInt(3));"
    },
    {
      "name": "a rival opens with two relics",
      "file": "Assets/GameAssets/Core/Run/VersusRun.cs",
      "find": "private const int OpeningKit = 3;",
      "replace": "private const int OpeningKit = 2;"
    },
    {
      "name": "a rival keeps the second of the two it looked at",
      "file": "Assets/GameAssets/Core/Run/VersusRun.cs",
      "find": "RelicId want = Wants(a) >= Wants(b)",
      "replace": "RelicId want = Wants(a) > Wants(b)"
    },
    {
      "name": "an unlisted relic outranks everything a rival wants",
      "file": "Assets/GameAssets/Core/Run/VersusRun.cs",
      "find": "private const int OpeningValueDefault = 3;",
      "replace": "private const int OpeningValueDefault = 10;"
    },
    {
      "name": "a rival may hold two of anything",
      "file": "Assets/GameAssets/Core/Run/VersusRun.cs",
      "find": "return rules.Stacks(id) || !rival.Relics.Contains(id);",
      "replace": "return true;"
    },
    {
      "name": "a rival's hall is not deepened by their level",
      "file": "Assets/GameAssets/Core/Run/VersusRun.cs",
      "find": "int reach = Math.Max(1, Math.Min(DungeonCatalog.All.Count, 1 + level / 3));",
      "replace": "int reach = 1;"
    },
    {
      "name": "a level above the hero's is worth no health",
      "file": "Assets/GameAssets/Core/Run/VersusRun.cs",
      "find": "Hp = heroPool + 3 * step + rng.NextInt(7) - 3,",
      "replace": "Hp = heroPool + rng.NextInt(7) - 3,"
    },
    {
      "name": "a rival's attack is never nudged upward at random",
      "file": "Assets/GameAssets/Core/Run/VersusRun.cs",
      "find": "Atk = 5 + (step > 0 ? 1 : 0) + (rng.Next() < 0.35 ? 1 : 0),",
      "replace": "Atk = 5 + (step > 0 ? 1 : 0) + (rng.Next() < 0.5 ? 1 : 0),"
    },
    {
      "name": "the arena opens on the hero's whole delve pool",
      "file": "Assets/GameAssets/Core/Run/VersusRun.cs",
      "find": "HeroPool = JsMath.RoundToInt((100 + bonuses.Hp) / 2.0),",
      "replace": "HeroPool = 100 + bonuses.Hp,"
    },
    {
      "name": "setting up deals no delve offer first",
      "file": "Assets/GameAssets/Core/Run/VersusRun.cs",
      "find": "            RelicDraft.RollOffer(new List<RelicId>(), 2 + bonuses.DraftChoices, rng,\n                rules, GameModes.Delve);",
      "replace": ""
    },
    {
      "name": "the discarded delve offer ignores the level-ten perk",
      "file": "Assets/GameAssets/Core/Run/VersusRun.cs",
      "find": "RelicDraft.RollOffer(new List<RelicId>(), 2 + bonuses.DraftChoices, rng,",
      "replace": "RelicDraft.RollOffer(new List<RelicId>(), 2, rng,"
    },
    {
      "name": "a face costs one fewer outfit slot",
      "file": "Assets/GameAssets/Core/Content/HeroWardrobe.cs",
      "find": "public const int DrawnSlots = 10;",
      "replace": "public const int DrawnSlots = 9;"
    },
    {
      "name": "a face costs one fewer colour family",
      "file": "Assets/GameAssets/Core/Content/HeroWardrobe.cs",
      "find": "public const int DrawnFamilies = 11;",
      "replace": "public const int DrawnFamilies = 10;"
    },
    {
      "name": "a face has no accent or motto",
      "file": "Assets/GameAssets/Core/Content/HeroWardrobe.cs",
      "find": "public const int DrawnFlourishes = 2;",
      "replace": "public const int DrawnFlourishes = 0;"
    },
    {
      "name": "the guard the source's duel keeps is gone",
      "file": "Assets/GameAssets/Core/Combat/CombatDamage.cs",
      "find": "if (rules.ReactionsStopWhenTheStrikerFalls && attacker.Php <= 0) return;",
      "replace": ""
    },
    {
      "name": "versus never took the delve's answer on a lethal thorn",
      "file": "Assets/GameAssets/Core/Combat/CombatRules.cs",
      "find": "public bool ReactionsStopWhenTheStrikerFalls;",
      "replace": "public bool ReactionsStopWhenTheStrikerFalls = true;"
    }
  ]
}
